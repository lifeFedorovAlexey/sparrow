{
  "name": "hermes-parser-ai",
  "version": "0.1.0",
  "private": true,
  "description": "Modular AI platform that designs, generates, validates, and maintains website parsers.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/app/cli.js",
    "visual": "node src/app/visual-editor/server.js",
    "test": "node --test",
    "test:watch": "node --test --watch"
  },
  "license": "MIT",
  "dependencies": {
    "cheerio": "^1.1.2",
    "playwright-core": "^1.54.2"
  }
}
